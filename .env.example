# URL base de la API. Si no se define, el front usa la de Render por defecto
# (ver src/api/api.js). Copia este archivo a .env para desarrollo local si
# necesitas apuntar a otro backend.
VITE_API_URL=https://umg-api-django.onrender.com/api
